#include <iostream>
#include <sstream>
#include <fstream>
#include <string>
#include <stdio.h>
#include <vector>

std::vector<std::vector<std::string> > load_annotations(std::string annotation_path);
